module.exports = {
  minDeposit: Math.pow(10, 17),
  maxDeposit: Math.pow(10, 18),
  ticketTtlSeconds: 300,
  joinFeePercentage: 1,
  minFee: 0.01 * Math.pow(10,18),
  maxFee: 0.01 * Math.pow(10,18)
};